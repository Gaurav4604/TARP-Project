import { Stack, Typography } from "@mui/material";
import React from "react";

const HeroSection = () => {
  return (
    <Stack id="hero-section">
      <Typography>Hello There</Typography>
    </Stack>
  );
};

export default HeroSection;
